language: java
install: true
after_success:
  - bash <(curl -s https://codecov.io/bash)
  - mvn verify -Dsonar.c.file.suffixes=- sonar:sonar

addons:
  sonarcloud:
    organization: "nealkumar"
    token: ${SONAR_TOKEN}

jdk:
  - openjdk8

script:
  - mvn clean verify sonar:sonar

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.sonar/cache'
